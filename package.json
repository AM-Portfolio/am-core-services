{
  "name": "am-core-services",
  "version": "1.0.0",
  "description": "AM Core Services Orchestrator Root",
  "scripts": {
    "info": "echo 'AM Core Services Monorepo' && echo 'Layers: libraries, domain, services'",

    "logs:clean": "node scripts/clean-logs.js",

    "build:compile":    "node scripts/run-with-logs.js mvn clean compile -s maven-settings.xml",
    "build:all":        "node scripts/run-with-logs.js mvn clean install -s maven-settings.xml -DskipTests",
    "build:all:tests":  "node scripts/run-with-logs.js mvn clean install -s maven-settings.xml",
    "clean":            "node scripts/run-with-logs.js mvn clean -s maven-settings.xml",
    "test":             "node scripts/run-with-logs.js mvn test  -s maven-settings.xml",

    "libs:compile":   "node scripts/run-with-logs.js npx -y dotenv-cli -e .env -- python scripts/mvn_build.py libraries/",
    "libs:build":     "node scripts/run-with-logs.js mvn clean install -pl libraries/am-analysis-adapter,libraries/am-analysis-client-lib,libraries/am-api-core-lib,libraries/am-cache-lib,libraries/am-common-event-model,libraries/am-common-lib,libraries/am-feign-lib,libraries/am-kafka-lib,libraries/am-market-client-lib,libraries/am-observability-lib,libraries/am-portfolio-client-lib,libraries/am-sdk-generator,libraries/am-sdk-verifier,libraries/am-security-lib,libraries/am-trade-client-lib -am -s maven-settings.xml -DskipTests",

    "lib:analysis-adapter:build":  "node scripts/run-with-logs.js mvn clean install -pl libraries/am-analysis-adapter     -am -s maven-settings.xml -DskipTests",
    "lib:analysis-client:build":   "node scripts/run-with-logs.js mvn clean install -pl libraries/am-analysis-client-lib  -am -s maven-settings.xml -DskipTests",
    "lib:api-core:build":          "node scripts/run-with-logs.js mvn clean install -pl libraries/am-api-core-lib         -am -s maven-settings.xml -DskipTests",
    "lib:cache:build":             "node scripts/run-with-logs.js mvn clean install -pl libraries/am-cache-lib            -am -s maven-settings.xml -DskipTests",
    "lib:common-event:build":      "node scripts/run-with-logs.js mvn clean install -pl libraries/am-common-event-model   -am -s maven-settings.xml -DskipTests",
    "lib:common:build":            "node scripts/run-with-logs.js mvn clean install -pl libraries/am-common-lib           -am -s maven-settings.xml -DskipTests",
    "lib:feign:build":             "node scripts/run-with-logs.js mvn clean install -pl libraries/am-feign-lib            -am -s maven-settings.xml -DskipTests",
    "lib:kafka:build":             "node scripts/run-with-logs.js mvn clean install -pl libraries/am-kafka-lib            -am -s maven-settings.xml -DskipTests",
    "lib:market-client:build":     "node scripts/run-with-logs.js mvn clean install -pl libraries/am-market-client-lib    -am -s maven-settings.xml -DskipTests",
    "lib:observability:build":     "node scripts/run-with-logs.js mvn clean install -pl libraries/am-observability-lib    -am -s maven-settings.xml -DskipTests",
    "lib:portfolio-client:build":  "node scripts/run-with-logs.js mvn clean install -pl libraries/am-portfolio-client-lib -am -s maven-settings.xml -DskipTests",
    "lib:sdk-generator:build":     "node scripts/run-with-logs.js mvn clean install -pl libraries/am-sdk-generator        -am -s maven-settings.xml -DskipTests",
    "lib:sdk-verifier:build":      "node scripts/run-with-logs.js mvn clean install -pl libraries/am-sdk-verifier         -am -s maven-settings.xml -DskipTests",
    "lib:security:build":          "node scripts/run-with-logs.js mvn clean install -pl libraries/am-security-lib         -am -s maven-settings.xml -DskipTests",
    "lib:trade-client:build":      "node scripts/run-with-logs.js mvn clean install -pl libraries/am-trade-client-lib     -am -s maven-settings.xml -DskipTests",

    "domain:compile": "node scripts/run-with-logs.js npx -y dotenv-cli -e .env -- python scripts/mvn_build.py domain/",
    "domain:build":   "node scripts/run-with-logs.js mvn clean install -pl domain -am -s maven-settings.xml -DskipTests",

    "services:compile":     "node scripts/run-with-logs.js npx -y dotenv-cli -e .env -- python scripts/mvn_build.py services/",
    "services:build":       "node scripts/run-with-logs.js mvn clean install -pl services/am-gateway,services/am-analysis,services/am-mcp-server -am -s maven-settings.xml -DskipTests",
    "services:build:tests": "node scripts/run-with-logs.js mvn clean install -pl services/am-gateway,services/am-analysis,services/am-mcp-server -am -s maven-settings.xml",

    "gateway:dev":     "node scripts/run-with-logs.js npx -y dotenv-cli -e services/am-gateway/.env         -- mvn spring-boot:run -pl services/am-gateway -s maven-settings.xml -Dspring-boot.run.profiles=dev",
    "gateway:preprod": "node scripts/run-with-logs.js npx -y dotenv-cli -e services/am-gateway/.env.preprod -- mvn spring-boot:run -pl services/am-gateway -s maven-settings.xml -Dspring-boot.run.profiles=preprod",
    "gateway:prod":    "node scripts/run-with-logs.js npx -y dotenv-cli -e services/am-gateway/.env.prod    -- mvn spring-boot:run -pl services/am-gateway -s maven-settings.xml -Dspring-boot.run.profiles=prod",
    "gateway:start":   "npm run gateway:dev",
    "gateway:build":   "node scripts/run-with-logs.js mvn clean install -pl services/am-gateway -am -s maven-settings.xml -DskipTests",
    "gateway:compile": "node scripts/run-with-logs.js mvn compile        -pl services/am-gateway -s maven-settings.xml",
    "gateway:test":    "node scripts/run-with-logs.js mvn test           -pl services/am-gateway -s maven-settings.xml",

    "analysis:dev":     "node scripts/run-with-logs.js npx -y dotenv-cli -e services/am-analysis/.env         -- mvn spring-boot:run -pl services/am-analysis -s maven-settings.xml -Dspring-boot.run.profiles=dev",
    "analysis:preprod": "node scripts/run-with-logs.js npx -y dotenv-cli -e services/am-analysis/.env.preprod -- mvn spring-boot:run -pl services/am-analysis -s maven-settings.xml -Dspring-boot.run.profiles=preprod",
    "analysis:prod":    "node scripts/run-with-logs.js npx -y dotenv-cli -e services/am-analysis/.env.prod    -- mvn spring-boot:run -pl services/am-analysis -s maven-settings.xml -Dspring-boot.run.profiles=prod",
    "analysis:start":   "npm run analysis:dev",
    "analysis:build":   "node scripts/run-with-logs.js mvn clean install -pl services/am-analysis -am -s maven-settings.xml -DskipTests",
    "analysis:compile": "node scripts/run-with-logs.js mvn compile        -pl services/am-analysis -s maven-settings.xml",
    "analysis:test":    "node scripts/run-with-logs.js mvn test           -pl services/am-analysis -s maven-settings.xml",

    "mcp:dev":     "node scripts/run-with-logs.js npx -y dotenv-cli -e services/am-mcp-server/.env         -- mvn spring-boot:run -pl services/am-mcp-server -s maven-settings.xml -Dspring-boot.run.profiles=dev",
    "mcp:preprod": "node scripts/run-with-logs.js npx -y dotenv-cli -e services/am-mcp-server/.env.preprod -- mvn spring-boot:run -pl services/am-mcp-server -s maven-settings.xml -Dspring-boot.run.profiles=preprod",
    "mcp:prod":    "node scripts/run-with-logs.js npx -y dotenv-cli -e services/am-mcp-server/.env.prod    -- mvn spring-boot:run -pl services/am-mcp-server -s maven-settings.xml -Dspring-boot.run.profiles=prod",
    "mcp:start":   "npm run mcp:dev",
    "mcp:build":   "node scripts/run-with-logs.js mvn clean install -pl services/am-mcp-server -am -s maven-settings.xml -DskipTests",
    "mcp:compile": "node scripts/run-with-logs.js mvn compile        -pl services/am-mcp-server -s maven-settings.xml",
    "mcp:test":    "node scripts/run-with-logs.js mvn test           -pl services/am-mcp-server -s maven-settings.xml",
    "mcp:inspect": "cd services/am-mcp-server && npm run inspect",

    "core:dev":   "node scripts/run-with-logs.js npx -y concurrently -n \"gateway,analysis,mcp\" -c \"cyan,magenta,yellow\" \"npm run gateway:dev\" \"npm run analysis:dev\" \"npm run mcp:dev\"",
    "core:start": "npm run core:dev",

    "infra:compose:up":   "docker-compose -f docker-compose.dev.yml up -d",
    "infra:compose:down": "docker-compose -f docker-compose.dev.yml down",
    "infra:compose:logs": "docker-compose -f docker-compose.dev.yml logs -f",

    "vault:sync": "python3 scripts/vault_sync.py --sync"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
