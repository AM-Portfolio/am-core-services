# Pre-production environment overrides for am-analysis
environment: preprod

replicaCount: 1

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi

# Vault enabled — role and token management now handled centrally in universal-chart
vault:
  enabled: true
  secretPaths:
    kafka: "apps/data/preprod/infra/kafka"
    mongodb: "apps/data/preprod/infra/mongodb"

# Non-secret env vars pointing to preprod cluster services
env:
  SPRING_PROFILES_ACTIVE: "preprod"
  KAFKA_BOOTSTRAP_SERVERS: "kafka.infra.svc.cluster.local:9092"
  KAFKA_SECURITY_PROTOCOL: "SASL_PLAINTEXT"
  KAFKA_SASL_MECHANISM: "SCRAM-SHA-256"
  MONGODB_CONNECTION_URL: "mongodb://mongodb.infra.svc.cluster.local:27017/analysis"
  MARKET_DATA_URL: "http://am-market-data.am-apps-preprod.svc.cluster.local:8092"
  REDIS_HOST: "redis.infra.svc.cluster.local"
  REDIS_PORT: "6379"
